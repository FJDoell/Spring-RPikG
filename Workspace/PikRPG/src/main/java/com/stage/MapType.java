package com.stage;

/**
 * SURFACE - Base is created.<br>
 * CAVE - Base is not created. When loaded, assumes that all captains are
 * present.
 * 
 * @author darkm
 *
 */
public enum MapType {
	SURFACE, CAVE;
}
